<Page
    x:Class="MedSystem.App.Pages.IcdReferencePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MedSystem.App.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="32,24,32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <TextBlock Text="Справочник МКБ" Style="{StaticResource TitleTextBlockStyle}" />
            <StackPanel Orientation="Horizontal" Spacing="12" HorizontalAlignment="Right">
                <TextBlock x:Name="CountText" Text="" VerticalAlignment="Center" Opacity="0.6" />
                <Button Content="+ Добавить" Style="{StaticResource AccentButtonStyle}"
                        Click="AddButton_Click" />
            </StackPanel>
        </Grid>

        <TextBox x:Name="SearchBox" Grid.Row="1" Margin="0,16,0,12"
                 PlaceholderText="Поиск по коду или названию..."
                 TextChanged="SearchBox_TextChanged" />

        <InfoBar x:Name="ErrorBar" Grid.Row="2" Margin="0,0,0,12"
                 IsOpen="False" Severity="Error" Title="Ошибка загрузки" />

        <Border Grid.Row="3" CornerRadius="8" BorderThickness="1"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Padding="16,12,16,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.4*" />
                        <ColumnDefinition Width="2*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Код" FontSize="12" Opacity="0.6" />
                    <TextBlock Grid.Column="1" Text="Наименование диагноза" FontSize="12" Opacity="0.6" />
                </Grid>
                <Rectangle Grid.Row="1" Height="1" HorizontalAlignment="Stretch"
                           Fill="{ThemeResource DividerStrokeColorDefaultBrush}" />
                <ListView x:Name="IcdList" Grid.Row="2" Margin="4,2,4,6" SelectionMode="Single"
                  IsDoubleTapEnabled="True" DoubleTapped="IcdList_DoubleTapped">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:IcdRow">
                    <Grid Padding="4,10" Background="Transparent">
                        <Grid.ContextFlyout>
                            <MenuFlyout>
                                <MenuFlyoutItem Text="Изменить"
                                                Tag="{x:Bind Code}" Click="EditMenuItem_Click" />
                                <MenuFlyoutSeparator />
                                <MenuFlyoutItem Text="Удалить"
                                                Tag="{x:Bind Code}" Click="DeleteMenuItem_Click" />
                            </MenuFlyout>
                        </Grid.ContextFlyout>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.4*" />
                            <ColumnDefinition Width="2*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Bind Code}" />
                        <TextBlock Grid.Column="1" Text="{x:Bind Name}" Opacity="0.8"
                                   TextTrimming="CharacterEllipsis" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
                </ListView>
                <ProgressRing x:Name="LoadingRing" Grid.Row="2" Width="40" Height="40"
                              IsActive="False" Visibility="Collapsed"
                              HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Grid>
        </Border>

        <Grid Grid.Row="4" Margin="0,12,0,0">
            <Button Content="К обращениям" MinWidth="130" HorizontalAlignment="Left"
                    Click="BackButton_Click" />
            <TextBlock x:Name="PageText" VerticalAlignment="Center"
                       HorizontalAlignment="Center" Opacity="0.7" />
            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                <Button x:Name="PreviousPageButton" Content="Назад"
                        Click="PreviousPageButton_Click" />
                <Button x:Name="NextPageButton" Content="Далее"
                        Click="NextPageButton_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
