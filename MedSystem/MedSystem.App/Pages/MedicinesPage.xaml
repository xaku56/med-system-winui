<Page
    x:Class="MedSystem.App.Pages.MedicinesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MedSystem.App.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="32,24,32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <TextBlock Text="Лекарства" Style="{StaticResource TitleTextBlockStyle}" />
            <StackPanel Orientation="Horizontal" Spacing="12" HorizontalAlignment="Right">
                <TextBlock x:Name="CountText" Text="" VerticalAlignment="Center" Opacity="0.6" />
                <Button Content="Заказать" Click="OrderButton_Click" />
                <Button Content="+ Добавить" Style="{StaticResource AccentButtonStyle}"
                        Click="AddButton_Click" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="0,16,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchBox"
                     PlaceholderText="Поиск по названию..."
                     TextChanged="SearchBox_TextChanged" />
            <ComboBox x:Name="FilterBox" Grid.Column="1" Margin="12,0,0,0"
                      MinWidth="200" SelectedIndex="0"
                      SelectionChanged="FilterBox_SelectionChanged">
                <x:String>Все лекарства</x:String>
                <x:String>Мало (&lt;= 5)</x:String>
                <x:String>Истекают (2 недели)</x:String>
                <x:String>Просроченные</x:String>
            </ComboBox>
        </Grid>

        <InfoBar x:Name="ErrorBar" Grid.Row="2" Margin="0,0,0,12"
                 IsOpen="False" Severity="Error" Title="Ошибка загрузки" />

        <Border Grid.Row="3" CornerRadius="8" BorderThickness="1"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Padding="16,12,16,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="0.7*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Название" FontSize="12" Opacity="0.6" />
                    <TextBlock Grid.Column="1" Text="Кол-во" FontSize="12" Opacity="0.6" />
                    <TextBlock Grid.Column="2" Text="Дозировка" FontSize="12" Opacity="0.6" />
                    <TextBlock Grid.Column="3" Text="Срок годности" FontSize="12" Opacity="0.6" />
                </Grid>
                <Rectangle Grid.Row="1" Height="1" HorizontalAlignment="Stretch"
                           Fill="{ThemeResource DividerStrokeColorDefaultBrush}" />
                <ListView x:Name="MedicinesList" Grid.Row="2" Margin="4,2,4,6" SelectionMode="Single"
                          IsDoubleTapEnabled="True" DoubleTapped="MedicinesList_DoubleTapped">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:MedicineRow">
                            <Grid Padding="4,10" Background="Transparent">
                                <Grid.ContextFlyout>
                                    <MenuFlyout>
                                        <MenuFlyoutItem Text="Открыть / Редактировать"
                                                        Tag="{x:Bind Id}" Click="OpenMenuItem_Click" />
                                        <MenuFlyoutSeparator />
                                        <MenuFlyoutItem Text="В корзину"
                                                        Tag="{x:Bind Id}" Click="DeleteMenuItem_Click" />
                                    </MenuFlyout>
                                </Grid.ContextFlyout>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*" />
                                    <ColumnDefinition Width="0.7*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{x:Bind Name}" VerticalAlignment="Center" />
                                <Border Grid.Column="1" Background="{x:Bind QtyBg}" CornerRadius="4"
                                Padding="10,2" HorizontalAlignment="Left" VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind Quantity}" Foreground="{x:Bind QtyFg}" FontSize="12" />
                        </Border>
                                <TextBlock Grid.Column="2" Text="{x:Bind Dosage}" Opacity="0.8" VerticalAlignment="Center" />
                                <Border Grid.Column="3" Background="{x:Bind DateBg}" CornerRadius="4"
                                Padding="10,2" HorizontalAlignment="Left" VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind ExpirationDate}" Foreground="{x:Bind DateFg}" FontSize="12" />
                        </Border>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <ProgressRing x:Name="LoadingRing" Grid.Row="2" Width="40" Height="40"
                              IsActive="False" Visibility="Collapsed"
                              HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Grid>
        </Border>

        <Grid Grid.Row="4" Margin="0,12,0,0">
            <TextBlock x:Name="PageText" VerticalAlignment="Center" Opacity="0.7" />
            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                <Button x:Name="PreviousPageButton" Content="Назад"
                        Click="PreviousPageButton_Click" />
                <Button x:Name="NextPageButton" Content="Далее"
                        Click="NextPageButton_Click" />
            </StackPanel>
        </Grid>

    </Grid>
</Page>
