<Page
    x:Class="MedSystem.App.Pages.GroupsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MedSystem.App.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="32,24,32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <TextBlock Text="Учебные группы" Style="{StaticResource TitleTextBlockStyle}" />
            <StackPanel Orientation="Horizontal" Spacing="12" HorizontalAlignment="Right">
                <Button Content="Перевести на следующий курс" Click="IncrementButton_Click" />
                <Button Content="+ Добавить" Style="{StaticResource AccentButtonStyle}"
                        Click="AddButton_Click" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Grid.RowSpan="2" Margin="0,16,0,0" CornerRadius="8" BorderThickness="1"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Padding="16,12,16,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Название" FontSize="12" Opacity="0.6" />
                    <TextBlock Grid.Column="1" Text="Студентов" FontSize="12" Opacity="0.6" />
                    <TextBlock Grid.Column="2" Text="Действия" FontSize="12" Opacity="0.6" />
                </Grid>
                <Rectangle Grid.Row="1" Height="1" HorizontalAlignment="Stretch"
                           Fill="{ThemeResource DividerStrokeColorDefaultBrush}" />
                <ListView x:Name="GroupsList" Grid.Row="2" Margin="4,2,4,6" SelectionMode="Single"
                  IsDoubleTapEnabled="True" DoubleTapped="GroupsList_DoubleTapped">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:GroupRow">
                    <Grid Padding="4,10" Background="Transparent">
                        <Grid.ContextFlyout>
                            <MenuFlyout>
                                <MenuFlyoutItem Text="Переименовать"
                                                Tag="{x:Bind Id}" Click="RenameMenuItem_Click" />
                                <MenuFlyoutSeparator />
                                <MenuFlyoutItem Text="В корзину"
                                                Tag="{x:Bind Id}" Click="DeleteMenuItem_Click" />
                            </MenuFlyout>
                        </Grid.ContextFlyout>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Bind Name}" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="1" Text="{x:Bind StudentCount}" Opacity="0.8"
                                   VerticalAlignment="Center" />
                        <Button Grid.Column="2" Content="Показать студентов" Tag="{x:Bind Id}"
                                Click="ShowStudentsButton_Click" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="12" Margin="0,12,0,0">
            <Button Content="Назад" MinWidth="110" Click="BackButton_Click" />
        </StackPanel>
    </Grid>
</Page>
