<Page
    x:Class="MedSystem.App.Pages.TrashPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MedSystem.App.Pages">

    <Grid Padding="32,24,32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <StackPanel Spacing="4">
                <TextBlock Text="Корзина" Style="{StaticResource TitleTextBlockStyle}" />
                <TextBlock x:Name="RetentionText" Opacity="0.6" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="12" HorizontalAlignment="Right">
                <TextBlock x:Name="CountText" VerticalAlignment="Center" Opacity="0.6" />
                <Button x:Name="EmptyTrashButton" Content="Очистить корзину"
                        Click="EmptyTrashButton_Click" />
            </StackPanel>
        </Grid>

        <InfoBar x:Name="ErrorBar" Grid.Row="1" Margin="0,12,0,0"
                 Severity="Error" IsOpen="False" IsClosable="True" />

        <Border Grid.Row="2" Margin="0,16,0,12" CornerRadius="8" BorderThickness="1"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Padding="16,12,16,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="170" />
                        <ColumnDefinition Width="150" />
                        <ColumnDefinition Width="220" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Тип" FontSize="12" Opacity="0.6" />
                    <TextBlock Grid.Column="1" Text="Запись" FontSize="12" Opacity="0.6" />
                    <TextBlock Grid.Column="2" Text="Удалено" FontSize="12" Opacity="0.6" />
                    <TextBlock Grid.Column="3" Text="Хранение" FontSize="12" Opacity="0.6" />
                </Grid>
                <Rectangle Grid.Row="1" Height="1" HorizontalAlignment="Stretch"
                           Fill="{ThemeResource DividerStrokeColorDefaultBrush}" />

                <Grid Grid.Row="2">
                    <ListView x:Name="TrashList" SelectionMode="None" Margin="4,2,4,6">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="local:TrashRow">
                                <Grid Padding="4,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="170" />
                                        <ColumnDefinition Width="150" />
                                        <ColumnDefinition Width="220" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{x:Bind TypeName}" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="1" Text="{x:Bind DisplayName}" VerticalAlignment="Center"
                                               TextTrimming="CharacterEllipsis" />
                                    <TextBlock Grid.Column="2" Text="{x:Bind DeletedAt}" VerticalAlignment="Center" Opacity="0.7" />
                                    <TextBlock Grid.Column="3" Text="{x:Bind Retention}" VerticalAlignment="Center" Opacity="0.7" />
                                    <StackPanel Grid.Column="4" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                                        <Button Content="Восстановить" Tag="{x:Bind Key}" Click="RestoreButton_Click" />
                                        <Button Content="Удалить" Tag="{x:Bind Key}" Click="DeleteButton_Click" />
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <ProgressRing x:Name="LoadingRing" Width="36" Height="36"
                                  HorizontalAlignment="Center" VerticalAlignment="Center"
                                  IsActive="False" Visibility="Collapsed" />
                    <TextBlock x:Name="EmptyText" Text="Корзина пуста"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               Opacity="0.6" Visibility="Collapsed" />
                </Grid>
            </Grid>
        </Border>

        <Grid Grid.Row="3">
            <Button x:Name="PreviousButton" Content="← Назад" Click="PreviousButton_Click" />
            <TextBlock x:Name="PageText" HorizontalAlignment="Center" VerticalAlignment="Center" />
            <Button x:Name="NextButton" Content="Вперёд →" HorizontalAlignment="Right"
                    Click="NextButton_Click" />
        </Grid>
    </Grid>
</Page>
